# WAOS 旺财环境变量样例
# 复制为 .env 并填入真实值。.env 文件已在 .gitignore 中，不会被提交。
#
# AUDIT-SEC-REL: 历史版本曾将 .env 提交到 git 仓库导致 ZHIPU_API_KEY 泄漏。
#                强烈建议：
#                  1. 不要在源码中硬编码 fallback key
#                  2. 生产环境用 secret manager（如 Vault / AWS Secrets Manager）
#                  3. 已泄漏的 key 必须立即吊销并轮换

# 智谱 GLM-4 官方 API Key（用于 /api/waos/brain 内置模型）
# 申请：https://open.bigmodel.cn/
ZHIPU_API_KEY=your_zhipu_api_key_here

# SQLite 数据库路径（默认相对工作目录）
DATABASE_URL=file:./db/custom.db

# 豆包 Docker 逆向服务地址（可选，启动 doubao2api Docker 后填入）
DOUBAO_DOCKER_URL=http://localhost:9090
